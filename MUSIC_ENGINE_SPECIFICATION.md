# Полная спецификация и архитектура музыкально-теоретического движка (Harmonic & Voice-Leading Engine)

> **Назначение документа:** Данный документ представляет собой исчерпывающую техническую и музыкально-теоретическую спецификацию алгоритмического движка гармонии, анализатора голосоведения, генератора академических задач, каденций и синтезатора звука. Документ составлен для реализации или портирования всей логики в любую другую систему, язык программирования (Python, C++, Rust, Swift, Go, C#) или платформу анализа музыкальных данных.

---

## Содержание

1. [Введение и архитектурные принципы](#1-введение-и-архитектурные-принципы)
2. [Математические основы и представление высот (Pitch Representation)](#2-математические-основы-и-представление-высот)
3. [Энгармоническая орфография и буквенно-слоговые системы](#3-энгармоническая-орфография-и-буквенно-слоговые-системы)
4. [Четырехголосная фактура SATB (Консерваторские стандарты)](#4-четырехголосная-фактура-satb)
5. [Анализатор голосоведения и проверка академических запретов](#5-анализатор-голосоведения-и-проверка-академических-запретов)
6. [Алгоритм оптимизации голосоведения (Voice-Leading Solver & Revoicing)](#6-алгоритм-оптимизации-голосоведения)
7. [Энциклопедия гармонических оборотов (10 фундаментальных классов)](#7-энциклопедия-гармонических-оборотов)
8. [Движок ладовой гармонии и режима «Тональность» (Tonal Engine)](#8-движок-ладовой-гармонии-и-режима-тональность)
9. [Схемы классических разрешений неустойчивых созвучий](#9-схемы-классических-разрешений-неустойчивых-созвучий)
10. [Движок гармонизации мелодии сопрано (Melody Harmonization Engine)](#10-движок-гармонизации-мелодии-сопрано)
11. [Звуковой движок и аддитивный синтез тембров (Audio Engine)](#11-звуковой-движок-и-аддитивный-синтез-тембров)
12. [Интеграция с нотной графикой (VexFlow Grand Staff Bridge)](#12-интеграция-с-нотной-графикой)
13. [План портирования и эталонные структуры данных (Reference Types)](#13-план-портирования-и-эталонные-структуры-данных)

---

## 1. Введение и архитектурные принципы

Движок разработан на основе академических правил классической гармонии (бригадный учебник гармонии под ред. И. В. Способина, труды Н. А. Римского-Корсакова, Ю. Н. Тюлина, И. И. Дубовского, С. В. Евсеева, В. В. Соколова).

### Ключевые возможности:
1. **Детерминированное четырехголосие (SATB):** каждый аккорд раскладывается на Сопрано, Альт, Тенор и Бас с учетом регистровых границ, тесного/широкого расположения и правил удвоения тонов.
2. **Анализатор ошибок (Linter голосоведения):** математическая проверка на параллельные квинты, параллельные октавы, движение всех голосов в одну сторону, перекрещивание голосов, заскакивание/пересечение звуков во времени, ходы на увеличенные интервалы и некорректное разрешение вводного тона и септим.
3. **Branch-and-Bound Солвер ревойсинга:** алгоритм ветвей и границ для нахождения идеального плавного голосоведения (наименьшее суммарное перемещение верхних голосов с сохранением общих звуков на месте).
4. **Библиотека 10 классов гармонических оборотов:** от базовых автентических и плагальных оборотов до альтерированных субдоминант, неаполитанских аккордов, золотых секвенций и тритоновых замен.
5. **Ладовый тренажер (Сольфеджио):** генерация созвучий в любой из 24 мажорных и минорных тональностей с мгновенным калькулятором ступеневого состава и автоматическим построением классического разрешения в тонику.
6. **Гармонизация сопрано:** система оценки студенческих задач по гармонии с поиском дефектов каденции и параллелизмов.
7. **Встроенный физико-акустический синтез:** Web Audio полифонический фортепианный генератор с обертонами, огибающими ADSR и раздельной панорамой голосов.

---

## 2. Математические основы и представление высот

Все высоты в движке представляются целыми числами по стандарту **MIDI Note Number** ($0 \dots 127$):
- $60$ — нота «До» первой октавы ($C_4$ / Middle C).
- Частота звука $f$ в Герцах рассчитывается по формуле равномерно-темперированного строя ($A_4 = 440\text{ Гц}$):
  $$f(m) = 440 \cdot 2^{\frac{m - 69}{12}}$$

### Высотный класс (Pitch Class, PC)
Каждый звук сводится к модулю 12:
$$PC(m) = ((m \bmod 12) + 12) \bmod 12$$
где:
$0 = C$, $1 = C\sharp/D\flat$, $2 = D$, $3 = E\flat/D\sharp$, $4 = E$, $5 = F$, $6 = F\sharp/G\flat$, $7 = G$, $8 = A\flat/G\sharp$, $9 = A$, $10 = B\flat/A\sharp$, $11 = B/H$.

### Ступеневые интервалы относительно тоники
Пусть $T_{PC}$ — высотный класс тоники ($C = 0, D = 2, \dots$):
$$\Delta_{PC}(m, T_{PC}) = ((PC(m) - T_{PC}) \bmod 12 + 12) \bmod 12$$

Таблица соответствия полутонов ступеням:
| $\Delta_{PC}$ | Мажор (Major) | Минор (Minor) | Название ступени |
|---|---|---|---|
| $0$ | I | I | Тоника |
| $1$ | $\flat$II | $\flat$II | Неаполитанская ступень |
| $2$ | II | II | Нисходящий вводный тон |
| $3$ | $\flat$III | III | Терцовый тон лада (минор) |
| $4$ | III | $\sharp$III | Терцовый тон лада (мажор) |
| $5$ | IV | IV | Субдоминанта |
| $6$ | $\sharp$IV / $\flat$V | $\sharp$IV | Тритон / альтерация |
| $7$ | V | V | Доминанта |
| $8$ | $\flat$VI | VI | Нисходящий вводный к доминанте (гарм. мажор / нат. минор) |
| $9$ | VI | $\sharp$VI | Натуральный мажор / дорийская секста |
| $10$ | $\flat$VII | VII | Натуральный минор |
| $11$ | VII | $\sharp$VII | Острый восходящий вводный тон |

---

## 3. Энгармоническая орфография и буквенно-слоговые системы

В движке реализована строгая классическая европейская (немецкая) и русская слоговая номенклатура.

### Буквенная система:
Базовые ступени: `C`, `D`, `E`, `F`, `G`, `A`, `H` (Си). `B` зарезервировано для Си-бемоль ($B\flat$).
- Суффикс диеза: `is` (`Cis`, `Dis`, `Fis`, `Gis`, `Ais`, `His`).
- Суффикс дубль-диеза: `isis` (`Cisis`, `Fisis`).
- Суффикс бемоля: `es` (`Des`, `Es`, `Fes`, `Ges`, `As`, `B`, `Ces`).
- Суффикс дубль-бемоля: `eses` (`Deses`, `Eses`, `Heses`).

### Алгоритм правильного нотописания ступени (`spellClassicalNoteName`):
1. Определяется буква базовой тоники (например, $C \to \text{индекс } 0$).
2. Находится теоретический номер ступени ($1 \dots 7$) через смещение $\Delta_{PC}$.
3. Вычисляется целевая буква: $\text{targetIdx} = (\text{tonicLetterIdx} + \text{degree} - 1) \bmod 7$.
4. Вычисляется разность полутонов между реальным MIDI высотным классом и натуральной нотой полученной буквы:
   $$\text{diff} = (PC - \text{naturalPC} + 18) \bmod 12 - 6$$
5. При $\text{diff} = 0 \to$ чистая буква; $+1 \to \text{буква} + \text{"is"}$; $+2 \to \text{"isis"}$; $-1 \to \text{правильный бемоль (As, Es, B, Des, \dots)}$.

---

## 4. Четырехголосная фактура SATB

Вся гармоническая ткань организуется строго в 4 голоса: кортеж `midisSATB: [B, T, A, S]`.

### Регистровые границы голосов (Диапазоны хора):
- **Сопрано (S):** $C_4 \dots A_5$ (MIDI $60 \dots 81$, предельно $C_6 = 84$).
- **Альт (A):** $G_3 \dots D_5$ (MIDI $55 \dots 74$, предельно $E_5 = 76$).
- **Тенор (T):** $C_3 \dots G_4$ (MIDI $48 \dots 67$, предельно $A_4 = 69$).
- **Бас (B):** $E_2 \dots C_4$ (MIDI $40 \dots 60$, предельно $D_4 = 62$).

### Правила расположения (Spacing):
1. **Тесное расположение (Close Spacing):**
   $$S - A \le 5 \text{ полутонов (кварта/терция)}, \quad A - T \le 5 \text{ полутонов}$$
2. **Широкое расположение (Open Spacing):**
   $$S - A \ge 5 \text{ полутонов (квинта/секста)}, \quad A - T \ge 5 \text{ полутонов}$$
3. **Ограничение расстояния между верхними голосами:**
   $$S - A \le 12 \text{ полутонов (октава)}, \quad A - T \le 12 \text{ полутонов (октава)}$$
   Превышение октавы между соседними верхними голосами категорически запрещено!
4. **Свобода басового голоса:**
   Расстояние $T - B$ может быть любым в пределах октавы с квинтой ($0 \dots 24$ полутона).

### Академические правила удвоения тонов:
- **Главные трезвучия ($T_{5/3}, S_{5/3}, D_{5/3}$):** строго удваивается прима аккорда.
- **Кадансовый квартсекстаккорд ($K_{6/4}$):** строго удваивается бас (квинта трезвучия = V ступень лада). Удвоение примы или терции в $K_{6/4}$ является грубой ошибкой.
- **Секстаккорды ($T_6, S_6, D_6$):** удваивается прима или квинта (основные ступени лада I, IV, V). Удвоение терции допускается только при плавном голосоведении в проходящих оборотах.
- **Секстаккорд II ступени ($II_6$):** строго удваивается бас (IV ступень лада — субдоминантовый тон).
- **Трезвучие VI ступени в прерванном обороте ($D_7 \to VI$):** в трезвучии VI ступени **строго удваивается терция** (тоника лада I ст.). Удвоение примы VI ступени запрещено.
- **Полный доминантсептаккорд ($D_7$):** содержит все 4 звука (прима, терция, квинта, септима).
- **Неполный $D_7$:** без квинты, удваивается прима (три примы + терция + септима).
- **Неполная тоника ($T_3$):** утроенная прима (три звука тоники) + одна терция.

---

## 5. Анализатор голосоведения и проверка академических запретов

Функция `checkVoiceLeading(steps)` производит полный статический и кинематический анализ последовательности созвучий.

### Математические критерии ошибок:

#### 1. Перекрещивание голосов (Voice Crossing)
В каждом такте должно строго выполняться неравенство:
$$B \le T \le A \le S$$
Если $B > T$, $T > A$ или $A > S$ — фиксируется дефект «Перекрещивание голосов».

#### 2. Параллельные квинты и октавы (Parallel Fifths & Octaves)
Для любых двух смежных шагов $s_1$ и $s_2$ и для любой пары голосов $(i, j)$, где $0 \le i < j \le 3$:
$$\Delta_1 = (m_{1,j} - m_{1,i} + 120) \bmod 12$$
$$\Delta_2 = (m_{2,j} - m_{2,i} + 120) \bmod 12$$
Если оба голоса двигались ($m_{2,i} \ne m_{1,i}$ и $m_{2,j} \ne m_{1,j}$):
- При $\Delta_1 = 7$ и $\Delta_2 = 7 \implies$ **Критическая ошибка: Параллельные чистые квинты**.
- При $\Delta_1 = 0$ и $\Delta_2 = 0 \implies$ **Критическая ошибка: Параллельные октавы / унисоны**.

#### 3. Движение всех 4 голосов в одну сторону (Direct Motion of All Voices)
Пусть $\text{dir}_k = \text{sign}(m_{2,k} - m_{1,k})$ для $k \in \{0, 1, 2, 3\}$.
Если $\text{dir}_0 = \text{dir}_1 = \text{dir}_2 = \text{dir}_3 \ne 0 \implies$ **Ошибка: Все 4 голоса движутся параллельно в одну сторону** (обеднение гармонии, потеря контрапункта).

#### 4. Заскакивание / Пересечение голосов во времени (Voice Overlapping)
Голос на втором шаге не должен переходить границу, на которой на предыдущем шаге находился соседний голос:
- Тенор поднялся выше прежнего звука Альта: $T_2 > A_1$
- Тенор опустился ниже прежнего звука Баса: $T_2 < B_1$
- Альт поднялся выше прежнего звука Сопрано: $A_2 > S_1$
- Альт опустился ниже прежнего звука Тенора: $A_2 < T_1$
- Сопрано опустилось ниже прежнего звука Альта: $S_2 < A_1$
- Бас поднялся выше прежнего звука Тенора: $B_2 > T_1$

#### 5. Разрешение септимы ($7\downarrow$)
Септима в любом септаккорде ($D_7, II_7, VII_7$) является диссонансом и обязана разрешаться **на секунду вниз** (на 1 или 2 полутона вниз в зависимости от ступени). Ход септимы вверх запрещен.

#### 6. Разрешение вводного тона (Leading-tone resolution)
Острый вводный тон (VII ступень в мажоре, $\sharp$VII в гармоническом миноре) в крайних голосах (в басу или в сопрано) обязан разрешаться **на полтона вверх в тонику (I ступень)**.

---

## 6. Алгоритм оптимизации голосоведения

Функция `revoiceProgression` позволяет перестроить исходный оборот в заданное расположение (`close`, `open`, `melodicPosition: octave/third/fifth`) с помощью алгоритма ветвей и границ (Branch-and-Bound).

### Математическая постановка задачи:
Дана последовательность $N$ аккордовых шагов. Для каждого шага $s$ генерируется множество допустимых кортежей кандидатов $C_s = \{[B_s, T_s, A_s, S_s]\}$, удовлетворяющих:
1. $B_s$ фиксирован гармонической функцией.
2. Высотные классы $T_s, A_s, S_s$ принадлежат множеству тонов аккорда.
3. Сохраняются регистровые ограничения: $48 \le T \le 69$, $55 \le A \le 76$, $60 \le S \le 84$.
4. $B \le T \le A \le S$, $A - T \le 12$, $S - A \le 12$.
5. На шаге $s = 0$ выполняются требования целевого расположения (тесное/широкое) и мелодического положения.

### Целевая функция штрафа (Loss Function):
Для перехода $s \to s+1$ штраф складывается из:
$$\text{Cost} = \sum_{v \in \{T, A, S\}} |m_{s+1,v} - m_{s,v}| - 2 \cdot \mathbb{I}(m_{s+1,v} = m_{s,v}) + 4 \cdot \mathbb{I}(\text{sign}(m_{s+1,v} - m_{s,v}) = \text{sign}(B_{s+1} - B_s))$$
- Первый член минимизирует общее расстояние ходов (закон плавного голосоведения).
- Второй член поощряет удержание общего звука на месте (бонус $-2$).
- Третий член штрафует прямое движение верхнего голоса в ту же сторону, куда идет бас ($+4$).
- Любое нарушение правил из Раздела 5 дает $\text{Cost} = \infty$ (немедленный prune ветви).

---

## 7. Энциклопедия гармонических оборотов

Движок содержит фундаментальную базу данных из 10 классов оборотов, скомпилированных с выверенными консерваторскими смещениями `satbOffsets: [B, T, A, S]` относительно тоники $C_4$ ($MIDI = 60$):

### 1. Кадансовые и базовые (Cadential)
- **Полный совершенный каданс:** $T_{5/3} - S_6 - K_{6/4} - D_7 - T_3$
  - Offsets:
    - $T_{5/3}$: `[-24, -8, -5, 0]` ($C_2, E_3, G_3, C_4$)
    - $S_6$: `[-21, -8, -3, 0]` ($E\flat_2 / E_2, E_3, A_3, C_4$)
    - $K_{6/4}$: `[-17, -8, -5, 0]` ($G_2, E_3, G_3, C_4$)
    - $D_7$: `[-17, -7, -5, 2]` ($G_2, F_3, G_3, D_4$)
    - $T_3$: `[-24, -12, 0, 0]` (неполная тоника с утроенной примой)
- **Превосходная каденция Рахманинова:** $t_{5/3} - s_{5/3} - s_6^{\text{harm}} - K_{6/4} - D_7 - t_3$
- **Каденция со второй субдоминантой:** $T_{5/3} - II_6 - K_{6/4} - D_7 - T_{5/3}$

### 2. Проходящие обороты (Passing)
- **Проходящий квартсекстаккорд:** $T_{5/3} - D_{6/4} - T_6$
  - Бас идет поступенно вверх: $I \to II \to III$.
  - Сопрано идет навстречу: $III \to II \to I$.
  - Тенор держит общий звук: $V = \text{const}$.
- **Субдоминантовый проходящий:** $S_{5/3} - T_{6/4} - S_6$

### 3. Вспомогательные обороты (Auxiliary)
- **Вспомогательный плагальный:** $T_{5/3} - S_{6/4} - T_{5/3}$
  - Бас на месте ($I$). Верхние голоса опевают: $I - IV - I$, $V - VI - V$, $III - IV - III$.
- **Вспомогательный автентический:** $D_{5/3} - T_{6/4} - D_{5/3}$

### 4. Прерванные обороты (Deceptive)
- **Классический прерванный каданс:** $T_{5/3} - S_6 - K_{6/4} - D_7 - VI_{5/3}$
  - Бас делает шаг на секунду вверх ($V \to VI$).
  - В $VI_{5/3}$ строго удваивается терция (звук тоники лада).
- **Прерванный оборот с расширением:** $D_7 \to VI_{5/3} \to S_6 \to D_7 \to T_{5/3}$.

### 5. Обороты со скачками голосов (Disjunct)
- **Скачки терций:** $T_{5/3} \to S_{5/3}$ со скачком от терции к терции в сопрано при противоположном движении баса.
- **Скачки квинт:** скачок между квинтами тоники и доминанты.

### 6. Альтерированные созвучия (Altered & Chromatic)
- **Неаполитанский секстаккорд ($N_6$ / $\flat II_6$):** мажорное трезвучие на низкой II ступени в обращении секстаккорда с удвоением баса (IV ст.).
- **Двойная доминанта ($DD_7$, $DD_{6/5}$):** Доминанта к доминанте с альтерацией ($\sharp IV$ ст.).
- **Итальянская, Французская и Немецкая увеличенная секста (Augmented 6th):**
  - Итальянская: $\flat VI - I - \sharp IV$
  - Французская: $\flat VI - I - II - \sharp IV$
  - Немецкая: $\flat VI - I - \flat III - \sharp IV$
- **Секста Чайковского:** мажорная субдоминанта в миноре с секстой ($s_6^{\sharp}$).

### 7. Модальные гармонии (Modal)
- **Дорийский оборот:** $t_{5/3} - S_{\text{дор}} - t_{5/3}$ (с высокой VI ступенью).
- **Фригийский каданс:** $t_{5/3} - s_6 - t_6 - D_{5/3}$ с басом, нисходящим по полутонам ($I \to \flat VII \to \flat VI \to V$).
- **Миксолидийский оборот:** $T_{5/3} - d_{\text{микс}} - T_{5/3}$ (натуральная малая септима).

### 8. Секвенции (Sequences)
- **Золотая секвенция (Квинтовый круг):** цепочка нисходящих квинт и восходящих кварт ($I - IV - VII - III - VI - II - V - I$).
- **Секвенция септаккордов:** $T_7 - S_7 - VII_7 - III_7 - VI_7 - II_7 - D_7 - T$.

### 9. Эллипсис и тритоновые замены (Ellipsis)
- **Тритоновая замена ($SubV_7$):** замена $D_7$ на септаккорд, отстоящий на тритон ($\flat II_7$), разрешающийся полутоновым сползанием всех голосов в $T_{5/3}$.
- **Доминантовый эллипсис:** переход диссонирующего неустойчивого аккорда непосредственно в другой неустойчивый без разрешения в тонику ($D_7 \to VII_7 \to II_7 \to D_7$).

### 10. Симметричные и современные лады (Modern)
- **Прокофьевская доминанта:** $D_7$ с внедрением низкой II ступени.
- **Гармония Шостаковича:** фригийские интонации с уменьшенными октавами.
- **Целотонные симметричные комплексы:** увеличенные трезвучия и созвучия из целых тонов.

---

## 8. Движок ладовой гармонии и режима «Тональность»

Режим «Тональность» предназначен для воспитания гармонического слуха в контексте ладовых тяготений.

### 4 функциональные группы (T, S, D, M):
1. **Тоника (T / Cyan):**
   - Устойчивый центр лада.
   - Аккорды: $T_{5/3}, T_6, T_{6/4}$.
2. **Субдоминанта (S / Amber):**
   - Направление движения от центра, ладовая мягкость.
   - Аккорды: $S_{5/3}, S_6, S_{6/4}, II_{5/3}, II_6, II_7$.
3. **Доминанта (D / Rose):**
   - Максимальное напряжение, острое стремление в тонику.
   - Аккорды: $D_{5/3}, D_6, D_{6/4}, D_7, D_{6/5}, D_{4/3}, D_2, VII_{5/3}, VII_7, K_{6/4}$.
4. **Медианты (M / Purple):**
   - Переменные, колористические функции.
   - Аккорды: $VI_{5/3}$ (нижняя медианта / субдоминантовая параллель), $III_{5/3}$ (верхняя медианта / доминантовая параллель).

### Обозначение обращений аккордов:
- Трезвучие: $5/3$ (основной вид)
- Секстаккорд: $6$ (терция в басу)
- Квартсекстаккорд: $6/4$ (квинта в басу)
- Септаккорд: $7$ (основной вид)
- Квинтсекстаккорд: $6/5$ (терция в басу)
- Терцквартаккорд: $4/3$ (квинта в басу)
- Секундаккорд: $2$ (септима в басу)

---

## 9. Схемы классических разрешений неустойчивых созвучий

Функция `resolveVoiceMidi` автоматически рассчитывает разрешение любого созвучия в устойчивую тонику:

### Формулы смещения голосов:
Пусть $\Delta = ((m - \text{TonicPitch}) \bmod 12 + 12) \bmod 12$:

1. **Вводный тон VII ступени ($\Delta = 11$):**
   $$\text{Resolution} = m + 1 \quad (\to I \text{ ступень})$$
2. **Септима IV ступени ($\Delta = 5$):**
   $$\text{Resolution} = m - 1 \text{ (в мажоре)} \quad \text{или} \quad m - 2 \text{ (в миноре)} \quad (\to III \text{ ступень})$$
3. **II ступень лада ($\Delta = 2$):**
   - В басу (например, в $D_{4/3}$): $\text{Resolution} = m + 2 \text{ (мажор)} / m + 1 \text{ (минор)} \quad (\to III \text{ ст.}, \text{образуя } T_6)$
   - В верхних голосах: $\text{Resolution} = m - 2 \quad (\to I \text{ ступень})$
4. **V ступень лада ($\Delta = 7$):**
   - В басу (при автентическом переходе): скачок $\text{Resolution} = m - 7 \text{ (или } m + 5) \quad (\to I \text{ ступень})$
   - В верхних голосах: остается на месте $\text{Resolution} = m$ (общий тон)
5. **VI ступень лада ($\Delta = 8 \text{ или } 9$):**
   $$\text{Resolution} = m - 2 \text{ (мажор)} / m - 1 \text{ (минор)} \quad (\to V \text{ ступень})$$
6. **Кадансовый $K_{6/4}$:**
   - Бас на V ступени остается на месте: $\text{Resolution} = m$
   - Четвертая ступень от баса ($I$ ст.) опускается в вводный тон: $m - 1$ ($VII$ ст.)
   - Шестая ступень от баса ($III$ ст.) опускается во вторую ступень: $m - 2$ ($II$ ст.)
   - Результат: $K_{6/4} \to D_7$ или $D_{5/3}$.

---

## 10. Движок гармонизации мелодии сопрано

Модуль гармонизации (`melodyHarmonization.ts`) анализирует заданную мелодическую строчку сопрано и проверяет гармонизацию пользователя.

### Алгоритм синтеза голосов по заданной мелодии (`buildSATBForChord`):
Для любого выбранного аккорда и известной ноты сопрано $S_{midi}$:
1. Бас $B$ рассчитывается по высоте регистра ($MIDI \in [41 \dots 58]$) на основе требуемого басового тона функции (прима, терция или квинта).
2. Тенор $T$ ($MIDI \in [48 \dots 67]$) берет недостающий тон или удваивает приму выше баса.
3. Альт $A$ ($MIDI \in [55 \dots 74]$) заполняет оставшийся аккордовый тон строго между тенором и сопрано.

### Система штрафов (Scoring System):
- **100 баллов** — базовая идеальная оценка.
- **$-20$ баллов** за каждый невыбранный аккорд в такте.
- **$-15$ баллов** за стилистически некорректный выбор функции для данной ноты мелодии.
- **$-15$ баллов** за незавершенную каденцию (отсутствие $T_{5/3}$ в конце).
- **$-10$ баллов** за слабую предыктовая подготовку (отсутствие $D$ или $K_{6/4}$ перед тоникой).
- **$-15$ баллов** за параллельные квинты между басом и сопрано.
- **$-15$ баллов** за параллельные октавы между крайними голосами.
- **Порог сдачи:** $\ge 80$ баллов.

---

## 11. Звуковой движок и аддитивный синтез тембров

Движок `audioEngine.ts` построен на низкоуровневом Web Audio API (`AudioContext`). Он не использует сэмплы, а синтезирует благородный акустический тембр рояля с помощью аддитивного синтеза обертонов.

### Формула синтеза звука клавиши:
Для каждой звучащей ноты создается группа генераторов:
1. **Основной тон ($f_0$):** синусоидальная волна высокой амплитуды.
2. **2-я гармоника ($2f_0$):** амплитуда $-6\text{ дБ}$, легкий фазовый сдвиг.
3. **3-я гармоника ($3f_0$):** амплитуда $-12\text{ дБ}$.
4. **4-я гармоника ($4f_0$):** амплитуда $-18\text{ дБ}$.
5. **Молоточковый транзиент (Hammer Attack Transient):** короткий всплеск отфильтрованного треугольного сигнала с полосовым фильтром (Bandpass Filter, $Q = 4.0$) длительностью $8\text{ мс}$.
6. **Огибающая громкости (ADSR):**
   - Attack: $0.005\text{ с}$ (мгновенная атака удара молоточка)
   - Decay: экспоненциальный спад
   - Sustain: затухание струны
   - Release: плавное глушение демпфером ($0.15\text{ с}$)

### Воспроизведение 4 голосов SATB (`playProgressionStep`):
Чтобы фактура звучала прозрачно, движок применяет весовые коэффициенты громкости:
- Бас ($B$): коэффициент усиления **$1.25$** (фундамент гармонии).
- Сопрано ($S$): коэффициент усиления **$1.15$** (мелодическая ведущая линия).
- Альт ($A$) и Тенор ($T$): коэффициент **$1.0$** (гармоническое заполнение).

### Режимы воспроизведения:
1. **Гармонический (Harmonic):** одновременная атака всех 4 голосов.
2. **Арпеджиато (Arpeggio):** последовательный запуск голосов с регулируемой задержкой $\Delta t = \frac{0.22}{\text{tempoMod}}\text{ с}$.
   - Направления: снизу вверх ($B \to T \to A \to S$), сверху вниз ($S \to A \to T \to B$), изнутри наружу ($T/A \to B/S$).
3. **Изоляция голоса:** солирование одного выбранного голоса (например, только Бас или только Альт) с приглушением остальных.

### Ладовая настройка (Key Cadence):
Перед выполнением заданий движок проигрывает формулу настройки:
$$\text{Каденция настройки:} \quad I_{5/3} \to IV_{6/4} \to I_{5/3} \to V_7 \to I_{5/3}$$
Это полностью настраивает слух пользователя на высотный центр лада.

---

## 12. Интеграция с нотной графикой

Модуль `ProgressionGrandStaff.tsx` конвертирует структуру `RealizedProgression` в нотную графику через библиотеку **VexFlow**:

### Правила нотного рендеринга:
1. **Двойной нотоносец (Grand Staff):**
   - Верхний нотоносец: Скрипичный ключ (`clef: 'treble'`). Содержит Сопрано (штиль вверх, `Stem.UP`) и Альт (штиль вниз, `Stem.DOWN`).
   - Нижний нотоносец: Басовый ключ (`clef: 'bass'`). Содержит Тенор (штиль вверх, `Stem.UP`) и Бас (штиль вниз, `Stem.DOWN`).
   - Нотоносцы объединяются фигурной акколадой (`StaveConnector.type.BRACE`) и начальной общей тактовой чертой.
2. **Ключевые знаки (Key Signature):**
   - Автоматически выставляются при ключе для всех 24 тональностей (`C`, `G`, `D`, `A`, `E`, `H`, `F#`, `F`, `Bb`, `Eb`, `Ab`, `Db` и минорные параллели).
3. **Лиги общих тонов (StaveTie):**
   - Если голос на смежных шагах сохраняет ту же высоту ($m_{s+1} = m_s$), между нотами отрисовывается соединительная лига.
4. **Индикатор септимы:**
   - Голос, несущий септиму, автоматически маркируется бейджем `7↓`.
5. **Римские и функциональные подписи:**
   - Под басовым нотоносцем точно под каждой нотой центрируется текстовая аннотация ($T_{5/3}, D_7, K_{6/4}$ и т.д.).
6. **Интерактивная подсветка:**
   - При воспроизведении шага активные ноты окрашиваются в цвет `#38bdf8` (неоновый циан).

---

## 13. План портирования и эталонные структуры данных

При переносе в Python, C++, Rust или другую среду используйте следующие интерфейсы данных (в синтаксисе TypeScript / JSON Schema):

```typescript
// 1. Шаг гармонического шаблона
interface ProgressionChordStep {
  symbol: string;               // "T5/3", "D7", "K6/4"
  degreeRoman: string;          // "I", "V", "I"
  nameRu: string;               // "Тоническое трезвучие"
  satbOffsets: [number, number, number, number]; // Смещения [B, T, A, S] от C4 (60)
  voiceDegreesRu: {
    soprano: string;
    alto: string;
    tenor: string;
    bass: string;
  };
}

// 2. Реализованный аккорд в конкретной тональности
interface RealizedProgressionStep {
  symbol: string;
  degreeRoman: string;
  nameRu: string;
  midisSATB: [number, number, number, number]; // Точные MIDI коды голосов
  noteNamesSATB: [string, string, string, string]; // "C3", "G3", "E4", "C5"
  voiceDegreesRu: {
    soprano: string;
    alto: string;
    tenor: string;
    bass: string;
  };
}

// 3. Результат анализа голосоведения
interface VoiceLeadingCheckResult {
  isValid: boolean;
  warnings: string[];
}

// 4. Задание ладового тренажера
interface SmartVoicingTask {
  baseMidi: number;          // MIDI тоники
  baseNoteName: string;      // "C", "F#"
  keyScaleMode: 'major' | 'minor';
  keyNameRu: string;         // "До мажор"
  chordName: string;         // "Доминантсептаккорд"
  chordInversionRu: string;  // "Основной вид (7)"
  chordNotesMidi: number[];  // [55, 59, 62, 65]
  chordNoteNames: string[];  // ["G3", "B3", "D4", "F4"]
  degreeRoman: string;       // "V7"
  functionSymbolRu: string;  // "D7"
  isRootPosition: boolean;
  voicingId: string;         // "v_D7"
  voicesBottomToTop: Array<{
    voiceName: string;
    midi: number;
    noteNameWithOctave: string;
    degreeRoman: string;
  }>;
}
```

### Чеклист для портирования движка:
1. [ ] Реализовать структуру нот и конвертер `midiToFreq` и `spellClassicalNoteName`.
2. [ ] Перенести константу `CLASSICAL_PROGRESSIONS` (массив шаблонов со смещениями `satbOffsets`).
3. [ ] Реализовать функцию `checkVoiceLeading` с проверкой 6 академических правил.
4. [ ] Реализовать алгоритм `revoiceProgression` (поиск по дереву состояний с фильтрацией штрафов).
5. [ ] Реализовать `createProgressionFromTonalTask` и `resolveVoiceMidi` для ладовых созвучий.
6. [ ] Подключить генератор звука (в Web Audio, JUCE, CoreAudio или numpy/scipy).
7. [ ] Реализовать нотную визуализацию (VexFlow, LilyPond, MusicXML или собственный рендерер холста).
